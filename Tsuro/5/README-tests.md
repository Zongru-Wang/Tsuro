For `xref`: 
run tests as follow:
`./xref < ref-tests/i-in.json`
where `i` is the number between 1 - 2.

For `xobs`: 
run tests as follow:
`./xobs < obs-tests/i-in.json`
where `i` is the number between 1 - 2.
